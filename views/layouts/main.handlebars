<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>AniMates</title>
        <link rel="stylesheet" href="/stylesheets/style.css">
        <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    </head>

    <body>
        <div class="wrapper">
            <header>
                <h1>
                    <p class="title">AniMates</p>
                </h1>
                <nav>
                    {{#if loggedIn}}
                    <a href="/" class="homepage">Home</a>
                    <a href="/" class="explorepage">Explore</a>
                    <a href="/" class="myprofilepage">My Profile</a>
                    <button id="logout" class="btn-no-style">Logout</button>
                    {{else}}
                    <a href="/" class="homepage">Home</a>
                    <a href="/login">Login</a>
                    {{/if}}
                </nav>
            </header>

            <main>
                {{{ body }}}
            </main>

        </div>
            {{!-- {{#if loggedIn}}
                <script src="#link to logout js"></script>
            {{/if}} --}}
    </body>

</html>